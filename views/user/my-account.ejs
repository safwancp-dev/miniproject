<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />

  <title> Apz Store My Account</title>

  <link href="https://fonts.googleapis.com/css?family=Montserrat:400,500,700&display=swap" rel="stylesheet" />
  <link type="text/css" rel="stylesheet" href="/css/bootstrap.min.css" />
  <link type="text/css" rel="stylesheet" href="/css/slick.css" />
  <link type="text/css" rel="stylesheet" href="/css/slick-theme.css" />
  <link type="text/css" rel="stylesheet" href="/css/nouislider.min.css" />
  <link rel="stylesheet" href="/css/font-awesome.min.css" />
  <link type="text/css" rel="stylesheet" href="/css/style.css" />

  <!-- Styles remain unchanged -->
  <style>
    body {
      background-color: #f7f9fc;
      font-family: 'Montserrat', sans-serif;
      color: #444;
      -webkit-font-smoothing: antialiased;
      -moz-osx-font-smoothing: grayscale;
      min-height: 100vh;
      padding-bottom: 40px;
    }
    h2 {
      font-weight: 700;
      color: #222;
    }

    .account-section {
      padding: 50px 15px 80px;
      max-width: 900px;
      margin: 0 auto;
    }

    .account-card {
      background: #fff;
      border-radius: 12px;
      box-shadow: 0 6px 18px rgba(0, 0, 0, 0.08);
      margin-bottom: 40px;
      overflow: hidden;
      transition: box-shadow 0.3s ease;
    }
    .account-card:hover {
      box-shadow: 0 10px 24px rgba(0, 0, 0, 0.12);
    }
    .account-card-header {
      background: #f0f3f7;
      padding: 22px 30px;
      font-size: 22px;
      font-weight: 700;
      color: #2c3e50;
      border-bottom: 1px solid #dce3ea;
      letter-spacing: 0.02em;
    }
    .account-card-body {
      padding: 30px;
    }

    /* Profile Info */
    .account-card-body p {
      font-size: 16px;
      margin-bottom: 12px;
      line-height: 1.5;
    }
    .account-card-body p strong {
      color: #222;
      font-weight: 600;
    }

    /* --- Form Controls --- */
    .form-group {
      margin-bottom: 20px;
    }
    .form-label {
      font-weight: 600;
      color: #555;
      margin-bottom: 8px;
      display: block;
      font-size: 15px;
    }
    .form-control {
      font-size: 16px;
      padding: 12px 16px;
      border-radius: 8px;
      border: 1.5px solid #ccc;
      transition: all 0.3s ease;
      box-shadow: none;
      width: 100%; /* Ensure inputs take full width */
    }
    .form-control:focus {
      border-color: #3a86ff;
      box-shadow: 0 0 0 0.25rem rgba(58, 134, 255, 0.25);
      outline: none;
    }

    /* --- Buttons --- */
    .btn {
      font-weight: 600;
      padding: 10px 18px;
      font-size: 16px;
      border-radius: 8px;
      transition: background-color 0.3s ease, border-color 0.3s ease, box-shadow 0.3s ease, transform 0.2s ease;
      display: inline-flex;
      align-items: center;
      gap: 8px;
      cursor: pointer;
      border: 1px solid transparent; /* Base border for all buttons */
    }
    .btn:hover {
      transform: translateY(-2px);
    }
    .btn-primary {
      background-color: #007bff; /* Vibrant Blue */
      border-color: #007bff;
      color: white;
      box-shadow: 0 4px 10px rgba(0, 123, 255, 0.25);
    }
    .btn-primary:hover {
      background-color: #0056b3;
      border-color: #0056b3;
      box-shadow: 0 6px 14px rgba(0, 123, 255, 0.4);
    }
    .btn-success {
      background-color: #28a745; /* Success Green */
      border-color: #28a745;
      color: white;
      box-shadow: 0 4px 10px rgba(40, 167, 69, 0.25);
    }
    .btn-success:hover {
      background-color: #218838;
      border-color: #218838;
      box-shadow: 0 6px 14px rgba(40, 167, 69, 0.4);
    }
    .btn-danger {
      background-color: #dc3545; /* Danger Red */
      border-color: #dc3545;
      color: white;
      box-shadow: 0 4px 10px rgba(220, 53, 69, 0.25);
    }
    .btn-danger:hover {
      background-color: #c82333;
      border-color: #c82333;
      box-shadow: 0 6px 14px rgba(220, 53, 69, 0.4);
    }
    .btn-outline-secondary {
      background-color: transparent;
      border-color: #6c757d;
      color: #6c757d;
      box-shadow: none;
    }
    .btn-outline-secondary:hover {
      background-color: #6c757d;
      color: #fff;
      border-color: #6c757d;
      box-shadow: 0 2px 6px rgba(108, 117, 125, 0.2);
    }
    .btn-sm {
      padding: 8px 14px;
      font-size: 14px;
      border-radius: 6px; /* Slightly smaller radius for small buttons */
      gap: 6px;
    }
    .btn-sm i {
      font-size: 14px;
    }

    /* Address form */
    form[action="/add-address"] {
      background: #f9fbfe;
      padding: 25px 30px;
      border-radius: 12px;
      border: 1px solid #e1e7f0;
      box-shadow: 0 2px 8px rgba(58, 134, 255, 0.1);
      margin-bottom: 30px;
    }
    form[action="/add-address"] .row > div {
      margin-bottom: 15px;
    }
    form[action="/add-address"] button {
      font-weight: 600;
      border-radius: 10px;
      padding: 12px 22px;
      font-size: 16px;
      box-shadow: 0 4px 10px rgba(58, 134, 255, 0.25);
    }
    form[action="/add-address"] button i {
      margin-right: 6px;
    }

    /* Address list */
    .address-list {
      list-style: none;
      padding: 0;
      margin: 0;
      display: grid;
      grid-template-columns: 1fr;
      gap: 20px;
    }
    @media (min-width: 576px) {
      .address-list {
        grid-template-columns: 1fr 1fr;
      }
    }
    .address-item {
      background: #fff;
      border-radius: 12px;
      padding: 25px 30px;
      box-shadow: 0 3px 12px rgba(0, 0, 0, 0.04);
      border: 1.5px solid #e5e8ec;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
      position: relative;
      transition: all 0.3s ease;
    }
    .address-item:hover {
      box-shadow: 0 6px 18px rgba(0, 0, 0, 0.08);
      border-color: #3a86ff;
    }
    .address-item.default-address {
      border-color: #34c759;
      background-color: #e9f7ef;
      box-shadow: 0 6px 20px rgba(52, 199, 89, 0.12);
    }
    .address-item.default-address::before {
      content: "DEFAULT";
      position: absolute;
      top: 16px;
      right: 16px;
      background-color: #34c759;
      color: white;
      font-weight: 700;
      font-size: 11px;
      padding: 5px 12px;
      border-radius: 12px;
      letter-spacing: 0.04em;
      text-transform: uppercase;
    }
    .address-details p {
      margin: 0 0 8px;
      font-size: 15px;
      color: #4a4a4a;
      line-height: 1.4;
    }
    .address-details p strong {
      color: #222;
      font-weight: 600;
    }

    /* Actions */
    .address-actions {
      margin-top: 18px;
      display: flex;
      gap: 14px;
      justify-content: flex-end;
      flex-wrap: wrap;
    }
    .address-actions form {
      margin: 0;
    }
    
    /* Modal Styling */
    .modal-content {
      border-radius: 12px;
      box-shadow: 0 7px 20px rgba(0, 0, 0, 0.2);
      font-size: 15px;
      font-weight: 500;
      color: #3c3c3c;
    }
    .modal-header {
      background-color: #f0f3f7;
      border-bottom: 1px solid #dce3ea;
      padding: 20px 30px;
      font-weight: 700;
      font-size: 20px;
      color: #34495e;
    }
    .modal-footer {
      padding: 20px 30px;
      border-top: 1px solid #dce3ea;
    }
    .modal-footer button {
      font-weight: 600;
      border-radius: 10px;
      padding: 10px 20px;
      font-size: 16px;
    }

    /* Alert */
    .alert-info {
      background-color: #e3f2fd;
      border-color: #90caf9;
      color: #1565c0;
      font-weight: 600;
      font-size: 16px;
      padding: 14px 20px;
      border-radius: 10px;
      margin-bottom: 40px;
    }

    /* Responsive */
    @media (max-width: 575px) {
      .account-section {
        padding: 30px 15px 50px;
      }
      .address-list {
        grid-template-columns: 1fr !important;
      }
      .address-actions {
        justify-content: flex-start;
      }
    }
  </style>
</head>
<body>

  <%- include('partials/header') %>

  <section class="account-section">
    <h2 class="text-center mb-5">My Account</h2>

    <% if (message) { %>
      <div class="alert alert-info text-center"><%= message %></div>
    <% } %>

    <% if (user) { %>
      <div class="account-card">
        <div class="account-card-header">Profile Information</div>
        <div class="account-card-body">
          <form action="/edit-user" method="POST">
            <div class="form-group">
              <label for="name" class="form-label">Name</label>
              <input type="text" id="name" name="name" value="<%= user.name %>" class="form-control" required />
            </div>

            <div class="form-group">
              <label for="email" class="form-label">Email</label>
              <input type="email" id="email" name="email" value="<%= user.email %>" class="form-control" required />
            </div>

            <button type="submit" class="btn btn-primary mt-3" aria-label="Submit Changes">
              <i class="fa fa-pencil"></i> Update Profile
            </button>
          </form>
        </div>
      </div>
    <% } %>

    <div class="account-card">
      <div class="account-card-header">My Addresses</div>
      <div class="account-card-body">
        <h5 class="mb-4">Add New Address</h5>
        <form action="/add-address" method="POST" class="mb-5" novalidate>
          <div class="row gx-3 gy-3">
            <div class="col-md-6">
              <input type="text" name="fullName" class="form-control" placeholder="Full Name" required />
            </div>
            <div class="col-md-6">
              <input type="text" name="phone" class="form-control" placeholder="Phone" required />
            </div>
            <div class="col-md-12">
              <input type="text" name="address" class="form-control" placeholder="Street Address, Apt, Suite, etc." required />
            </div>
            <div class="col-md-6">
              <input type="text" name="city" class="form-control" placeholder="City" required />
            </div>
            <div class="col-md-6">
              <input type="text" name="postalCode" class="form-control" placeholder="Postal Code" required />
            </div>
          </div>
          <button type="submit" class="btn btn-primary mt-4">
            <i class="fa fa-plus-circle"></i> Add Address
          </button>
        </form>

        <% if (addresses && addresses.length > 0) { %>
          <ul class="address-list">
            <% addresses.forEach(address => { %>
              <li class="address-item <%= address.isDefault ? 'default-address' : '' %>">
                <div class="address-details">
                  <p><strong><%= address.fullName %></strong></p>
                  <p><%= address.address %></p>
                  <p><%= address.city %> - <%= address.postalCode %></p>
                  <p>Phone: <%= address.phone %></p>
                </div>
                <div class="address-actions">
                  <!-- Removed Set Default button -->
                  <form
                    action="/delete-address"
                    method="POST"
                    onsubmit="return confirm('Are you sure you want to delete this address?');"
                    class="d-inline-block"
                    aria-label="Delete address"
                  >
                    <input type="hidden" name="addressId" value="<%= address._id %>" />
                    <button type="submit" class="btn btn-sm btn-danger">
                      <i class="fa fa-trash"></i> Delete
                    </button>
                  </form>
                </div>
              </li>
            <% }); %>
          </ul>
        <% } else { %>
          <p class="text-center text-muted fs-5 mt-4">You haven't added any addresses yet.</p>
        <% } %>
      </div>
    </div>
  </section>

  <%- include('partials/footer') %>

  <script src="/js/jquery.min.js"></script>
  <script src="/js/bootstrap.bundle.min.js"></script>
  <script src="/js/slick.min.js"></script>
  <script src="/js/nouislider.min.js"></script>
  <script src="/js/jquery.zoom.min.js"></script>
  <script src="/js/main.js"></script>
</body>
</html>
